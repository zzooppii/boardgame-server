import { marsCard, marsCardDescription, MARS_TAG_NAMES, MARS_CARD_RESOURCE_NAMES, type MarsCard, type MarsDefinition } from '@hangul-rummikub/shared';

export function MarsArt({cell,className=''}:{cell:number;className?:string}){return <span aria-hidden="true" className={'tm-art '+className} style={{backgroundPosition:`${cell%4*100/3}% ${Math.floor(cell/4)*50}%`}}/>;}
export function marsScoreLabel(d:MarsDefinition){return d.score==='resources'&&d.resourceScore?`${MARS_CARD_RESOURCE_NAMES[d.resource??'']??'자원'} ${d.resourceScore.per}개당 ${d.resourceScore.points} VP`:d.score==='jovian'?'목성 태그당 1 VP':d.score==='cities'?'전체 도시 3개당 1 VP':d.score==='capital'?'인접 해양당 1 VP':d.score==='life'?'과학 자원 보유 시 3 VP':d.points?`${d.points} VP`:'';}
export function MarsCardView({card,selected=false,reason=null,cost,onSelect,disabled=false}:{card:MarsCard;selected?:boolean;reason?:string|null;cost?:number;onSelect():void;disabled?:boolean}){const d=marsCard(card.definitionId);return <button className={`tm-card ${d.type} ${selected?'selected':''}`} aria-pressed={selected} disabled={disabled} onClick={onSelect} aria-label={`${d.name} · ${cost??d.cost} M€${reason?' · '+reason:''}`}><div className="tm-card-top"><b>{cost??d.cost}<small>M€</small></b><strong>{d.name}</strong></div><MarsArt cell={d.art}/><span className="tm-card-tags">{d.tags.map((t,i)=><span key={i}>{MARS_TAG_NAMES[t]}</span>)}</span><span className="tm-card-description">{marsCardDescription(d)}</span><span className="tm-card-footer"><span>{d.type==='event'?'이벤트':d.type==='active'?'지속·행동':'자동화'} · #{d.number}</span><b>{marsScoreLabel(d)}</b></span>{reason&&<span className="tm-card-blocked">{reason}</span>}{d.resource&&<span className="tm-card-counter">{MARS_CARD_RESOURCE_NAMES[d.resource]??'자원'} {card.resources}</span>}</button>;}
