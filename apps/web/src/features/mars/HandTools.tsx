import {MARS_TAG_NAMES} from '@hangul-rummikub/shared';
import {DEFAULT_MARS_HAND_OPTIONS,MARS_HAND_SORTS,type MarsHandOptions} from './hand.js';
export function HandTools({options,onChange,count,total}:{options:MarsHandOptions;onChange(value:MarsHandOptions):void;count:number;total:number}){
 return <div className="tm-hand-tools"><label>카드 검색<input type="search" value={options.query} placeholder="이름·효과·카드 번호" onChange={e=>onChange({...options,query:e.target.value})}/></label><label>태그<select aria-label="태그" value={options.tag} onChange={e=>onChange({...options,tag:e.target.value})}><option value="">모든 태그</option>{Object.entries(MARS_TAG_NAMES).map(([id,name])=><option key={id} value={id}>{name}</option>)}</select></label><label>카드 종류<select aria-label="카드 종류" value={options.kind} onChange={e=>onChange({...options,kind:e.target.value})}><option value="">모든 종류</option><option value="automated">자동화</option><option value="active">지속·행동</option><option value="event">이벤트</option></select></label><label>손패 정렬<select aria-label="손패 정렬" value={options.sort} onChange={e=>onChange({...options,sort:MARS_HAND_SORTS.find(s=>s.id===e.target.value)?.id??'received'})}>{MARS_HAND_SORTS.map(s=><option key={s.id} value={s.id}>{s.label}</option>)}</select></label><label className="tm-hand-check"><input type="checkbox" checked={options.eligibleOnly} onChange={e=>onChange({...options,eligibleOnly:e.target.checked})}/>조건·비용 충족</label><button onClick={()=>onChange({...DEFAULT_MARS_HAND_OPTIONS})}>필터 초기화</button><span role="status">{count} / {total}장</span></div>;
}
