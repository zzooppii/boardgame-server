# P19D — 그림 릴레이 Web

Server checkpoint: `2f025f4`. Web now advertises five implemented games. DRAW has a concrete route rather than a renderer registry. Its Lobby is 3–8 seats with Host-only EASY/NORMAL/MIXED configuration.

Original sketchbook SVG/CSS, paper canvas, marker palette and synchronized public book pages are scoped to DRAW. Native Pointer Events use bounded integer logical coordinates (1000×700), eight colors and three widths. Pen/eraser/undo/confirmed clear are local editor operations; whole private drafts autosave after a 300ms quiet period with one in-flight save and caller-owned draft revision. Failed acknowledgements retain the command identity for retry and preserve local artwork. Only acknowledged artwork is guaranteed across refresh. Submit locks editing.

Guess is escaped text, limited to 40 Unicode characters. Waiting reveals only completion status. Reveal shows only the server-projected prefix, with original/final comparison after the last contribution. Finished has no score, rank or winner. Host rematch reuses Room/session identity; scope changes are ordered by roomRevision and gameId.

First-use five-step guide, persistent sound preference, original short synthesized sounds, text equivalents, focus outlines and reduced-motion CSS. No new dependency or external artwork/audio.

Local production browser: two actual viewers plus one raw participant completed DRAW→GUESS→REVEAL→FINISHED→same-Room Lobby (`GATNMP`, disposable local test). Three pointer-drawn strokes restored after refresh as `저장됨 · 3/250 획`. Host/non-host controls verified. 1280 desktop, 390 DRAW and 320 GUESS/Reveal inspected; no horizontal overflow. A 320 timer line-wrap was identified and corrected with nowrap. Both viewers' console warning/error logs were empty before rebuilding. P20 rechecks the final bundle and expands race/lifecycle coverage.

P19D gate: typecheck/test/build/diff-check PASS, 1645 tests (110 shared / 417 Web / 1118 server). Existing >500KB chunk warning remains. Railway NOT DEPLOYED. P20 follows this checkpoint.
## Mobile drawing focus mode

An unsubmitted DRAW editor offers ‘그림 크게 그리기’ and opens it automatically on mobile as described below. The same mounted editor/canvas expands into a fixed dynamic-viewport dialog: private prompt and existing server-derived countdown above, compact tool/color rows and submission/save status below. Canvas remains 1000×700 with proportional fitting, not stretched coordinates. The underlying document is scroll-locked; portrait tools do not require horizontal scrolling. Close/Escape restores previous scroll/focus and leaves local drawing, pending autosave and submission identity intact. Background siblings are inert while focused; Tab stays in the dialog. Cleanup also runs on stage change/submission/unmount. No timer, protocol, privacy or drawing-data changes; existing reduced-motion styling applies.
Local production verification: 390×844 and 320×568 portrait, 844×390 landscape. Pointer-drawn stroke remained after close/reopen; save status confirmed one stroke. Body scroll lock restored on close and successful submit, and no inert background elements remained. Final landscape layout places tools beside the canvas. Browser console warnings/errors: zero. Physical touch/stylus feel still requires device playtest. Root quality gate: 1656 tests (110 shared / 419 Web / 1127 server), typecheck/build/diff-check PASS; existing large-bundle warning retained. No commit/push/deployment in this UI task.

### Mobile default drawing workspace

Unsubmitted DRAW editors now start in focus mode on viewports up to 768px wide, or coarse-pointer landscape devices up to 500px tall. This is chosen once per editor/stage: folding the workspace keeps it folded until reopened, and rotation does not reset the drawing or user choice. Desktop retains manual entry. The focused toolbar exposes pen, eraser, a labeled width selector and undo in one row, with all eight colors below (two rows on narrow phones). Clear remains confirmation-protected beside save/submit in the footer. Landscape keeps a two-column tool panel beside the proportional canvas. Select controls participate in the existing keyboard focus trap. No protocol, game rules or drawing coordinate changes.

Verification for this update: local Chromium fixture at 390×844, 320×568, 844×390 and 1280×900 confirmed mobile automatic entry, desktop manual entry, visible drawing controls/submit, width selection, a saved stroke surviving fold/reopen, clear cancellation, and scroll/inert cleanup. No browser page errors. Draw relay UI tests: 11/11 pass. Physical device touch feel remains to be checked. Root test rerun with local-port permission stopped at two unrelated Spirit Island UI assertions expecting 12 expansion fear cards while the concurrent implementation exposes 15; later root test stages did not run in that attempt.
Final root typecheck/build reruns also failed in the concurrently changing Terrorscape server contracts (including missing TerrorTrackingSchema, immobilized and RESET_TRAPS); web typecheck/build passed, with the existing >500KB bundle warning. git diff --check passed. No deployment performed.
