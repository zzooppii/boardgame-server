# 7 Wonders Duel 아키텍처

- `packages/shared/src/games/seven-wonders-duel`: 공개 구성물, 직렬화 DTO, 설정, SELECT 명령.
- `apps/server/src/games/seven-wonders-duel/domain`: 전체 비공개 상태, 시대 배치, 비용, 효과 큐, 승리 판정. 난수·시간·ID를 주입한다.
- application은 방별 직렬화, 인증, 멱등성, room/game revision 및 transition ID를 검사하고 검증된 candidate만 commit한다.
- compatibility projection은 시점별 공개 정보와 viewer 자신의 선택만 내보낸다. 신화 덱, 제거된 비공개 카드, 상대 음모, 숨겨진 법령은 원시 상태로 전송하지 않는다.
- 추가 선택은 서버 task queue에 저장된다. 본행동, 효과의 대상 선택, 카드 공개, 턴 인계를 구분한다. 재접속은 동일 projection과 선택 목록을 복원한다.
- 웹은 테이블 배치, 카드 확대, 선택 확인, 연계 참고표, 이벤트 기반 음향을 담당한다. 프론트가 판정을 수행하지 않는다. 비공개 정보의 사운드나 애니메이션도 공개 이벤트만 사용한다.
- 음향은 사용자 조작 이후 Web Audio로 합성하고 음소거를 제공한다. reduced-motion, 키보드 및 터치 조작을 지원한다.

기존 room/session 수명주기와 server-authoritative 경계는 유지한다. 전용 테이블 및 자산은 게임 선택 후 로드한다.

## 완료 검증 (2026-09-15)

- root `npm run typecheck`: 통과.
- root `npm test`: 공유 133, 웹 877, 서버 3,802, E2E 155 — 총 4,967건 통과.
- root `npm run build`: 통과. 기존 공통 JavaScript 번들의 500 kB 초과 경고는 유지된다. 듀얼 화면은 약 26 kB의 별도 지연 로딩 청크다.
- `git diff --check`, 신규 파일 공백 및 문서 내부 링크 확인: 통과.
- 듀얼 규칙·통신 및 공통 방 테스트 재검증: 143건 통과. 기본판 및 확장 네 조합의 48개 시드 대전, 모든 신·음모의 효과 처리, 비용, 과학 쌍, 군사·원로원, 비공개 정보, 멱등성, 재접속, 취소 및 같은 방 재시작을 포함한다.
- Chrome 두 독립 접속: 방 생성/입장, 불가사의 선택, 카드 확대/확정, 2시대 진입, 판테온 공개, 관문 활성화 및 두 화면에 동일한 신 후보 5장 공개를 확인했다. 관문 활성화 자체는 배치 카드를 소비하지 않았다.
- 1440px 데스크톱 및 390px 모바일 확인. 모바일 문서 너비 390px, 브라우저 JavaScript 오류 없음. 카드 배치는 독립 영역에서 좌우 스크롤한다.
- 일러스트 3개 atlas(총 48개 장면)의 출처와 생성 프롬프트는 `apps/web/public/images/seven-wonders-duel/README.md`에 기록했다.

## 추가 검수 (2026-09-15)

- 구현 커밋: `841390f`.
- 발견 및 수정: 턴 시작 음모로 배치가 비면 본행동을 기다리며 멈추던 문제. 효과 큐가 모두 끝난 시점에 시대 전환/최종 점수 계산을 진행한다. Agora 단독·동시 확장에서 1시대 및 3시대 회귀 테스트로 재현 후 수정했다.
- 발견 및 수정: 두 번째 플레이어가 선택하는 미네르바 목적지의 내/상대 수도 안내 방향. 두 자리 모두 확인한다.
- 복합 규칙 검수: 과학 쌍 동시 획득은 활성 플레이어부터 선택하며, 교환으로 쌍이 해체되어도 획득한 진보는 보존한다. 과학 승리는 남은 영향력 효과보다 먼저 게임을 종료한다.
- 듀얼 domain 68건 통과(기존 64건 + 회귀/예외 4건).
- 두 Chrome 접속의 관문 공개 후보 5장 일치, 모달 초점 및 Escape 닫기, 음량 저장·음소거·미리 듣기, 모바일 문서 너비 390px, JavaScript 오류 없음.
- 실제 Web Audio 검사: 미리 듣기 음원 4개 생성 → 음소거 시 추가 생성 없음 → 재활성화 시 4개 생성. 별도 검사 스크립트의 잘못된 닉네임(공백)으로 초기 대기 실패가 있었고, 유효한 입력으로 고친 뒤 통과했다.
- 이번 검수는 명시한 회귀·복합 상황에 대한 검증이다. 가능한 모든 카드 조합의 전수 검증이나 정식 서버 배포를 의미하지 않는다.
- 최종 root 검증: `npm run typecheck`, `npm test`, `npm run build` 모두 통과. 공유 133 + 웹 880 + 서버 3,806 + E2E 155 = 총 4,974건, 실패·skip 없음. 기존 공통 번들 500 kB 초과 경고 유지. `git diff --check` 통과.

## 복합 규칙 및 전체 플레이 품질 검수 (2026-09-15)

### 규칙과 선택 경계

- 하데스 → 정치가 고용 → 방패 법령 → 군사 토큰의 영향력 효과 → 남은 정치가 행동 순서를 회귀 테스트로 고정했다. 남은 정치가 행동이 군사 토큰 효과보다 앞서던 오류를 수정했다.
- 신들의 극장 → 이시스 → 쿠리아 율리아 → 미준비 음모 → 신화 덱 순서 복원 및 추가 턴을 검증한다.
- 관문 → 엔키 → 법률/이슈타르 과학 쌍, 넵투누스 군사 토큰의 정치 승리, 협박과 아스타르테 보호 코인, 강요의 양쪽 동시 과학 승리, 아누비스 → 이시스 → 영묘 재건 및 버린 건물 부활을 검증한다.
- 비공개 카드 선택은 행과 행 내 위치를 함께 표시한다. 확대 화면은 실제 선택한 대상에 해당하는 행동만 보여주며, 단순히 `definitionId === null`인 다른 대상은 포함하지 않는다.
- 판테온 활성화 선택의 공개 `sourceId`는 `pantheon-<자리>`로 구별한다. 의회 위치의 숫자 ID와 화면상 연결이 겹치지 않도록 한 변경이며 서버 SELECT/revision 검증은 동일하다.

### 화면 개선

- 건설·버리기 선택과 공개 기록에 카드 이름을 표시한다.
- 모바일에서 현재 선택 안내를 고정하고 효과 선택 목록으로 이동하는 링크를 제공한다.
- 보유 건물·토큰의 작은 글씨와 흰 의원 카드의 대비를 개선하고 신화 이름을 한국어로 표시한다.
- 군사 우세의 주체와 승리 사유를 한국어로 표시한다. 종료 후 비어 있는 카드 배치의 높이를 줄여 양쪽 도시를 이어서 볼 수 있게 한다.
- 로딩 최적화·정식 배포는 이번 범위에 포함하지 않는다.

### 두 접속의 전체 UI 대전 결과

1440px 데스크톱과 390px 모바일의 독립 Chrome 접속을 사용했다. 사람이 두 명 참여한 사용성 조사 대신, 서버가 각 접속에 공개한 합법적 선택을 실제 화면의 카드·확대·확정 버튼으로 실행하는 자동 대전이다.

| 구성 | 선택 횟수 | 완료 범위 | 판테온 활성화 | 음모 준비/발동 |
| --- | ---: | --- | ---: | ---: |
| 기본판 | 73 | 불가사의 선택 → 1·2·3시대 → 점수 종료 | 0 | 0/0 |
| Pantheon | 92 | 불가사의 선택 → 1·2·3시대 → 점수 종료 | 6 | 0/0 |
| Agora | 113 | 불가사의 선택 → 1·2·3시대 → 점수 종료 | 0 | 3/3 |
| Pantheon + Agora | 135 | 불가사의 선택 → 1·2·3시대 → 점수 종료 | 6 | 1/1 |

- 총 413회 선택, 모든 게임의 양쪽 결과 일치. 각 게임의 효과 선택 중 한 번씩 새로고침·재접속 후 진행을 마쳤다.
- 네 게임 모두 모바일 문서 너비 390px, JavaScript 오류 및 일러스트 응답 오류 없음.
- 실제 Web Audio 음원 생성과 카드 공개 클래스 전환을 관찰했다. 모바일의 reduced-motion 설정에서 확대 창 애니메이션이 비활성화됨을 확인했다.
- 최종 데스크톱/모바일 및 확대 화면을 캡처해 가독성과 종료 후 도시 배치를 확인했다.
- 검사 스크립트의 초기 확장 체크박스 대기 방식과 이름이 같은 의원 카드 선택 방식을 수정했다. 최종 실행은 서버 응답을 기다리고 실제 카드의 배치 위치를 구별해 네 게임 모두 완료했다.

초기 root 테스트를 브라우저 대전과 병행했을 때 다른 게임의 `SNEAKY eight raw clients / 15-second real-time tapping`이 처리 횟수 128회(최소 160회)로 한 번 실패했다. 브라우저 검수가 끝난 뒤 root 테스트를 다시 실행한 결과 같은 검사는 544회, 최대 ACK 335ms로 통과했다. 해당 게임의 코드나 테스트 기준은 변경하지 않았다.

최종 root 검증: `npm test` 4,988건(공유 133 + 웹 886 + 서버 3,814 + E2E 155) 모두 통과, 실패·skip 없음. `npm run typecheck`, `npm run build`, `git diff --check` 통과. 듀얼 domain은 기존 68건에서 76건으로 늘었다. 빌드의 기존 공통 번들 500 kB 초과 경고는 유지된다.

## 불가사의 선택 화면의 비용·효과 안내 (2026-09-16)

- 선택 카드의 작은 자원 기호를 자원 이름과 수량(`파피루스 ×2` 등)으로 바꾸고, 승점뿐 아니라 방패·코인·생산·특수 효과를 함께 표시한다.
- `wonder-info.ts`는 공개 카탈로그를 바탕으로 표시용 효과 설명을 만든다. `WonderCard.tsx`의 선택 카드, 마우스/키보드 툴팁, 클릭/터치 상세 창이 같은 설명을 사용한다. 보유한 불가사의에도 같은 툴팁을 제공한다.
- 자원은 나중에 건설할 때 필요함을 안내한다. Agora의 선택 즉시 효과와 건설 효과는 별도 제목으로 구분한다. 실제 구매 가격 계산이나 게임 규칙은 변경하지 않는다.
- 툴팁은 화면 경계 안에 배치하며 포인터를 설명 위로 옮겨도 유지한다. Escape, 초점 이탈, 화면 스크롤로 닫힌다. 터치는 상세 창을 열며, 기존 행동 선택과 명시적 확정 절차를 유지한다.
- 모바일 선택 카드는 한 열로 표시해 비용과 효과를 자르지 않는다. 공개된 불가사의만 렌더링하며 숨겨진 선택 후보는 추가로 노출하지 않는다.
- 회귀 검증: 제우스상의 제거/방패/승점 및 비용, 콜로소스의 방패 2개, 등대의 선택 생산, 아르테미스 신전의 코인/추가 턴, 16개 불가사의의 선택 즉시 효과 구분.
- 독립 Chrome 두 접속으로 1440px 마우스·키보드 툴팁, 설명 위 포인터 유지/Escape, 390px 모바일 터치·가로 넘침 없음, 열람 중 revision 유지와 명시적 확정 후 양쪽 revision 갱신을 확인했다. JavaScript 오류 없음.
- `npm run build` 통과(기존 공통 번들 500 kB 경고 유지). root 타입 검사는 병행 작업 중인 ArnakResults의 PlayerId 타입 및 화성 기업 공유 export 오류로 초기 실패했으나, 해당 작업의 수정 후 `npm run typecheck` 재실행에서 통과했다. 듀얼 변경으로 해당 파일을 수정하지 않았다.
- 브라우저 검수의 첫 실행은 이전 로컬 서버와 최신 웹 빌드의 불일치로 초기 입장에서 중단됐다. 최신 서버를 별도 로컬 포트로 실행한 뒤 동일 검사 전체가 통과했다.
- 최종 root `npm test`: 공유 133 + 웹 946 + 서버 3,940 + E2E 155 = 총 5,174건 통과, 실패·skip 없음. `git diff --check` 통과. 듀얼 UI 테스트 6건 중 이번 회귀 테스트는 2건이다.

## 군사력 확대 및 시대별 건물 연계도 (2026-09-16)

- `MilitaryView.tsx`: 군사력 요약을 버튼으로 열어 19칸 세로 트랙, 현재 충돌 말, 양쪽 수도, 미네르바, 남은/사라진 토큰을 표시한다. 항상 상대 수도를 위에 배치하고 viewer 자리 기준으로 방향을 바꾼다.
- 기본판/Pantheon은 중앙에서 3칸의 2코인 손실과 6칸의 5코인 손실을 명시한다. Agora에서는 코인 손실 대신 영향력 배치, 자신의 큐브 이동 후 상대 큐브 제거를 표시한다. 일반 코인 부족, 1회성 토큰, 군사 승점 2/5/10 및 수도 도달 즉시 승리도 설명한다.
- `building-reference.ts`, `BuildingReference.tsx`: 공개 카탈로그의 chainIn/chainOut으로 시대별 연결선을 만든다. 3단 연계 및 시대 I→III 연결을 보존한다. 연계되지 않은 건물/조합은 별도 표로 표시하며 Pantheon의 대신전·신화 토큰, Agora 의원의 고용 비용 안내를 구별한다.
- 비용은 자원 이름·수량, 효과는 한국어로 표시한다. 확대 연계표의 카드 클릭은 기존 상세 창으로 이어지고 모바일에서는 표 내부만 가로 스크롤한다. 실제 게임에서 제외된 카드도 포함하는 공개 구성물 참고표임을 안내한다.
- 서버 규칙·공유 계약·비공개 정보·선택 명령을 바꾸지 않는 읽기 전용 화면 변경이다. 첨부 이미지는 트랙 방향과 연계표 배치의 참고로 사용했다.
- 회귀 검사: 양쪽 자리의 방향 대칭, 토큰 제거 상태, Agora 교체 효과, 3단/시대 건너뛰기 연계, 네 확장 조합의 모든 구성물 중복·누락 없음.
- Chrome 두 독립 접속의 기본판/동시 확장, 1680px PC/390px 모바일에서 19칸·현재 위치, 연계선, 내부 가로 스크롤, 카드 상세 이동, Escape/닫기, 열람 전후 revision 유지 및 JavaScript 오류 없음을 확인했다.
- 초기 브라우저 검수에서 병행 빌드 중 임시로 웹 진입점이 사라져 중단됐다. 빌드 자산을 별도 로컬 경로에 고정한 뒤 두 구성 전체 검사를 통과했다.
- 신규 회귀 테스트가 특정 시대가 없는 Agora 의원(age 0)의 실제 화면 누락을 잡았다. 의원을 별도 영역으로 표시한 뒤, 네 조합에서 모델의 구성물 보존뿐 아니라 실제 렌더링 카드 수까지 확인한다. 브라우저에서도 의원 13개의 고용 안내 표시를 검증했다.
- 초기 전체 타입 검사/빌드는 병행 화성 작업의 MarsEffect 타입 및 미사용 marsCorporation import로 실패했다. 해당 작업 수정 후 최종 root 빌드는 통과했다. 기존 공통 번들 500 kB 경고는 유지된다.
- 최종 root 검증: `npm test` 공유 139 + 웹 949 + 서버 3,950 + E2E 155 = 5,193건 통과, 실패·skip 없음. `npm run typecheck`, `npm run build`, `git diff --check` 통과. 이번 군사력/연계도 회귀 테스트 2건을 포함해 듀얼 UI 테스트는 8건이다.

## 보유 도시의 혜택과 건물 미리보기 (2026-09-16)

- `CityPanel.tsx`는 보유 건물을 색상별 세로 묶음으로 표시한다. 카드 이름보다 생산량·구매 할인·과학 기호·방패·승점을 먼저 표시하고, 고정 생산 합계와 서버의 실제 과학 기호 보유 목록을 함께 보여준다.
- `building-info.ts`는 표시용 정보를 공개 카탈로그에서 읽는다. 건물 고정 생산 합계에 선택 생산·구매 할인·불가사의 효과를 더하지 않는다. 선택 생산은 여러 자원의 동시 생산과 구분하며, 건설 시 코인 보상은 일회성으로 명시한다. 현재 코인은 서버 값 그대로 표시한다.
- `BuildingDetails.tsx`는 생산·할인·과학, 건설 시 효과, 종료 점수, 무료 연계를 분리한다. 중앙 카드에는 서버가 제공한 실제 건설 비용(0 포함)을 표시하고 보유 건물 설명에는 현재 구매 견적을 표시하지 않는다.
- `CardTooltip.tsx`의 공통 미리보기를 보유 건물, 중앙의 공개 카드, 보유 효과 토큰과 기존 불가사의에 적용한다. 큰 일러스트와 설명을 표시하며 마우스 이동·키보드 초점·Escape를 지원한다. 터치는 기존 상세 창을 열고, 비공개 카드는 미리보기 내용을 제공하지 않는다.
- 모바일은 도시 내부에서 색상 묶음을 가로 스크롤한다. 중앙 배치 높이는 남은 카드 위치를 기준으로 잡아 이미 비워진 아래쪽 공간을 줄인다. 구매·선택은 기존 확인 단계를 유지하며 서버 계약과 게임 판정은 변경하지 않는다.
- 검수: 듀얼 UI 회귀 10건 통과. 새 검사는 생산 중복 합산, 선택 생산/할인의 제외, 일회성 수입, 0코인 견적, 네 확장 조합의 색상 분류와 무료 연계를 확인한다.
- 기본판의 독립 Chrome 두 접속(1680px/390px)에서 불가사의 선택부터 시대 II까지 38회 실제 선택 후 보유 효과 표시를 확인했다. 큰 툴팁의 마우스 이동 유지, 키보드 초점, Escape, 중앙 카드 비용, 비공개 카드 미노출, 기존 불가사의 툴팁, 모바일 상세 창과 문서 너비, 열람 전후 revision 불변 및 JavaScript 오류 없음도 확인했다. 이는 자동 브라우저 검수이며 사람 두 명의 사용성 평가는 아니다.
- 최초 검사에서 테스트 fixture의 branded TileId 및 카드 뒷면 타입 오류를 수정했다. 별도 웹 타입 검사에서 병행 화성 작업의 미사용 import 오류도 관찰했으며 이후 웹 빌드에서는 해소됐다.
- 최종 root 검증: `npm test` 공유 140 + 웹 953 + 서버 3,955 + E2E 155 = 5,203건 통과, 실패·skip 없음. `npm run typecheck`, `npm run build`, `git diff --check` 통과. 기존 공통 번들 500 kB 초과 경고는 유지된다.

## 듀얼 턴 제한시간 (2026-09-16)

- 방장만 대기실에서 제한 없음 또는 30/60/90초를 선택하며 기본값은 60초다. `DuelSettingsSchema`는 그 밖의 값과 임의 추가 필드를 거절한다. 기존 설정 payload에서 시간 항목을 생략하면 60초로 정규화한다. 시작 후에는 설정을 바꿀 수 없다.
- 서버 `DuelState`에 `deadlineAt`, `turnNumber`, 두 플레이어의 `timerRemaining`을 저장한다. 턴 종료 및 추가 턴에서 새 예산을 만들고, 본행동·같은 플레이어의 효과는 잔여 예산을 공유한다. 효과 중 선택자가 바뀌면 이전 선택자의 잔여 시간을 저장하고 상대의 예산으로 전환한다. 불가사의 선택과 그 즉시 효과는 한 예산을 공유한다.
- `applyDuelAction`은 서버 Clock 기준 기한 도달을 거절한다. `timeoutDuel`은 공개된 일반 행동에서 버리기를 우선하고, 선택적 효과는 생략하며, 필수 선택은 서버의 합법 선택 목록에서 첫 항목을 사용한다. 만료된 같은 예산의 연속 효과는 후보 상태에서 모두 처리한 뒤 한 번 commit한다. 비공개 선택의 기존 기록 마스킹을 유지한다.
- `DuelService`가 게임·revision·turnId·deadline을 검증하고 room mutation executor 안에서 자동/수동 처리를 직렬화한다. 기존 시계·scheduler·overdue sweeper 인프라에 듀얼 capability를 연결하며, 다른 게임의 시간 초과 정책은 바꾸지 않는다. adapter가 activeTurn을 노출해 누락된 타이머를 복구하고, 오래된 콜백은 NO_OP 처리한다.
- 진행 projection에는 `deadlineAt`만 추가하고 내부 시간 예산은 노출하지 않는다. 웹은 서버 시각 표본과 단조 증가 로컬 시간으로 남은 초를 표시한다. 화면 이동·재접속은 서버 기한을 초기화하지 않으며, 0초에는 서버 처리 대기를 안내한다.
- 회귀 검수: 듀얼 domain 85건(시간 관련 9건 포함), 소켓 4건, 웹 11건. 30/60/90초의 정확한 만료 경계, 상대 선택 중 예산 보존, 추가 턴 초기화, 필수 효과의 연속 자동 처리, 네 확장 조합의 전 과정 자동 종료를 검증한다. 설정 권한·잘못된 시간 값·만료 입력·중복 타이머 경합도 검사한다.
- Chrome PC/390px 모바일 두 접속에서 방장 설정 90→60→30초 동기화, 참가자 설정 잠금, 실제 30초 경과에 따른 불가사의 선택 및 일반 행동 버리기/코인 지급, 재접속 시 deadline 유지, 상세 창 자동 닫힘, JavaScript 오류 없음과 문서 너비를 확인했다. 라디오 버튼의 테스트는 서버 응답 후 반영되는 상태를 기다리도록 조정했다.
- 현재 작업 트리의 병행 Great Kingdom/Carcassonne 작업에서 미완성 화면 import·공유 타입·미사용 변수로 전체 검사가 중단됐다. 해당 파일을 수정하지 않고 기존 커밋과 듀얼 변경만 포함한 임시 사본에서 root 검증 및 브라우저 테스트를 수행했다. 사본 구성 시 공통 라우터에 섞인 다른 게임 변경을 제거해 듀얼 연결만 포함했고, 누락된 UI 테스트 설정 fixture도 갱신했다.
- 최종 분리 사본의 root `npm test`: 공유 139 + 웹 952 + 서버 3,960 + E2E 155 = 5,206건 통과, 실패·skip 없음. root `npm run typecheck`, `npm run build` 통과. 기존 공통 번들 500 kB 경고는 유지된다. 원본 작업 트리의 듀얼 소스가 검증 사본과 동일함을 확인했고 `git diff --check`도 통과했다.

### 제한 없음 추가 (2026-09-16 사용자 요청)

방장이 제한 없음 또는 30/60/90초를 선택한다. 기본값 60초는 유지한다. 제한 없음은 설정값 0, 서버 기한 null, 잔여 예산 [0,0]으로 표현하며 타이머 예약·복구 목록에서 제외한다. 시간 경과로 수동 선택을 거절하거나 자동 선택하지 않는다. 대기실·게임·상세 창에서 제한 없음임을 안내한다.

- 제한 없음 검수: 서버 시각을 하루 뒤로 진행해도 수동 선택이 허용되고, 동시 확장 게임 전체에서 deadline=null과 예산 [0,0]이 유지된다. 시작·선택 후에도 scheduler 복구 목록에 등록되지 않음을 소켓 검사로 확인했다. UI 12건과 실제 PC/390px 모바일의 설정 동기화·참가자 설정 잠금·재접속·상세 창·수동 선택·카운트다운 부재 검사도 통과했다.
- 이번 변경은 원본 작업 트리의 root 빌드도 통과했다. 분리 사본의 root 타입 검사/빌드와 별도 UI 회귀 검사를 통과했으며, 원본 듀얼 소스와 검증 사본의 일치를 확인했다. 기존 공통 번들 크기 경고는 유지된다.
- 분리 사본 root `npm test`: 공유 139 + 웹 952 + 서버 3,962 + E2E 155 = 5,208건 통과, 실패·skip 없음. 이후 추가한 제한 없음 렌더링 검사를 포함해 듀얼 UI 12건을 별도로 재컴파일·실행했고 모두 통과했다.

## 불가사의 건설 완료 표시

- 양쪽 플레이어가 공통으로 사용하는 `WonderCard`에 서버의 `built` 상태를 따라 금색 체크, 상단의 ‘건설 완료’ 띠, 금색 테두리를 지속 표시한다. 제거된 카드와 선택 단계에는 완료 배지를 표시하지 않는다.
- 카드의 접근성 이름에도 현재 건설 상태를 포함하며, 비용·효과 열람 동작과 게임 규칙은 유지한다.
- 로컬 카드 브라우저 검수에서 완료/미건설/제거/선택 단계의 배지 구분 및 390px 너비의 가로 넘침 없음을 확인했다. root 타입 검사와 웹 테스트는 통과했다. root 전체 테스트는 병행 작업인 Speakeasy의 공유 export 누락으로 서버 테스트 컴파일 단계에서 중단됐다.
- 이후 root 빌드도 통과했다. 기존 공통 번들 500 kB 초과 경고는 유지된다. 전체 테스트 중단 결과는 별도로 남기며 통과로 간주하지 않는다.
