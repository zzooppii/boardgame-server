# Arnak 구현

## 교역소 구매 비용 가독성 — 2026-09-16

- 교역소 카드 하단에 `구매 · 금화`/`구매 · 나침반`과 24px 수량을 고정 배지로 표시한다. 아이템은 금색, 유물은 초록색 배경이며 자원 이름도 함께 표시해 색상·기호만으로 구분하지 않는다. 점수는 배지 옆에 분리한다.
- 교역소 카드 폭·높이를 데스크톱/모바일 각각 확대하고 그림·가격 영역이 축소되지 않게 한다. 손패 카드와 실제 구매 비용·서버 판단은 변경하지 않는다. 카드 접근성 이름에도 구매 자원·수량을 포함한다.
- 브라우저 회귀는 두 화면의 여섯 가격 배지·자원 이름·수량 글자 크기·내부 넘침을 확인하고, 기존 구매 검사가 큰 가격 숫자를 읽어 실제 차감과 비교하도록 갱신했다. 데스크톱 교역소 이미지도 직접 확인했다.
- 빌드와 2·3·4인 Chrome 회귀 3건(약 63초)이 통과했다. 최초 브라우저 실행은 동시 빌드 중 웹 파일 교체로 검증 서버 시작이 실패했으며, 파일 생성 후 서버를 다시 실행해 통과했다. 기존 사용자 게임 서버는 재시작하지 않았다.
- 루트 타입 검사는 범위 밖 `in-memory-persistence.ts`의 `input` 참조 오류로 실패했다. 이후 빌드는 통과했으며 기존 번들 경고는 유지된다. 전체 test는 공유 144·웹 981건 통과, 서버 4,105건 중 4,104건 통과·Mars 에너지 판매/CEO 복사 검사 1건 실패로 종료되어 후속 Space Crew E2E는 실행되지 않았다. 해당 범위 밖 파일은 수정하지 않았다. 변경 범위 공백 검사와 브라우저 스크립트 구문 검사는 통과했다.

기존 게임별 domain/application/compatibility 경계를 따른다. room의 기존 인증·직렬 실행·멱등성·재접속을 재사용한다.

## 고급 연구·조수 승급 도메인 회귀 — 2026-09-16

- `apps/server/src/arnak.research.test.ts`에서 2·3·4인 참가자가 돋보기와 수첩을 출발점에서 7단계까지 번갈아 진행한다. 첫 두 조수 고용, 수첩 3·4단계의 각각 한 명 승급, 수첩의 사원 진입 금지, 돋보기 사원 도착 순서·보너스 소진을 검사한다.
- 모두 11점 사원 타일을 구매해 실제 비용 차감과 타일 소진 후 선택지 제거를 확인한다. 정상 패스·라운드 전환으로 5라운드를 끝내고 연구 점수 33/31/30/29점(사원 도착 순위 점수와 수첩 7단계 10점)과 사원 타일 점수·합계를 대조한다.
- 별도 중반 상태에서 이미 사용한 은색 금광 탐사자를 연구 보상으로 승급한다. 다시 준비됨, 본인·상대 projection의 금색 상태 일치, 금화 3개 지급, 중복 사용 불가, 자유 행동 후 연구 주 행동 유지와 차례 종료를 확인한다.
- 테스트는 처음에만 자원 또는 중반 상태를 준비한다. 이후에는 서버가 생성한 행동 ID로 `applyArnakAction`을 호출하고 매 행동의 원본 상태 불변·상태 보존 조건을 검사한다. 초기 자원이 많은 규칙 회귀이며 실제 브라우저 대국이나 자연스러운 자원 수급 전략을 검증한 것은 아니다. 모든 조수의 금색 효과 조합·고급 연구 브라우저 검증은 후속 범위로 유지한다.
- 신규 4건은 약 0.6초에 통과했다. 게임 규칙·UI·서버 동작·DTO·dependency 변경은 없다.
- 루트 `typecheck`·`build`·`test` 통과. 공유 144·웹 981·서버 4,085·E2E 155, 총 5,365건 실패 0건이다. 기존 번들 크기 경고는 유지되며 변경 범위 공백 검사도 통과했다. 이 작업에서 아르낙 브라우저 대국을 추가 실행하지 않았다.

서버가 현재 상태에서 가능한 행동 목록을 생성한다. 클라이언트는 선택한 행동 ID와 gameRevision/turnId를 보낸다. 서버는 현재 목록을 다시 생성해 검증한 뒤 복제 상태에 적용하고 한 번에 저장한다. 비용 선택과 중간 효과 선택도 동일한 방식이다.

비공개 손패와 덱은 서버에 보관한다. 상대방 projection에는 개수만 공개하고 본인에게만 손패와 행동 목록을 보낸다. 발견 전 장소·수호자·덱 순서는 전송하지 않는다.

화면은 중앙 탐험 섬, 오른쪽 연구 트랙, 상단 시장, 하단 개인 캠프와 손패로 구성한다. 일러스트는 새로 생성한 island/camp/atlas를 사용하며, 소리는 사용자 입력 이후 Web Audio로 합성한다. 음소거와 모션 감소 설정을 존중한다.

## 검증 범위

- 기본판 75장 각각 사용 및 중간 효과 해소, 카드 인스턴스 보존.
- 2·3·4인, 고정 난수 4개씩 총 12개 대국의 5라운드 종료.
- 위조 행동, 차례 위반, 오래된 revision, 멱등 재전송, 재접속 후 선택 복구, 상대 손패와 덱 비공개.
- 데스크톱 실제 배치 조작 및 390px 모바일에서 문서 가로 넘침 없음 확인.

이는 자동화된 규칙·상태 검증이며 모든 카드 조합의 플레이테스트를 대체하지 않는다. 확장은 아직 구현하지 않았다. 초판 카드 이름 번역과 테마 일러스트는 원작과 차이가 있다.

## 2026-09-15 최종 검증 기록

- 루트 `npm run typecheck`: 성공.
- 루트 `npm run build`: 성공. 기존 통합 프런트엔드 번들의 500kB 초과 경고가 남아 있다.
- 루트 `npm test`: shared 133/133, web 894/894, server 3841/3844 통과. 별도로 수정 중인 Ark Nova의 2·3·4인 Socket.IO 시작 테스트 3건이 `INTERNAL_ERROR`로 실패했다. Arnak 관련 실패는 없다.
- 마지막 비공개 선택 기록 수정 후 Arnak 서버 domain/integration 9/9, 웹 2/2 재검증 성공.
- 루트 테스트가 앞선 실패에서 중단하므로 `node --test e2e/space-crew*.test.mjs`를 별도로 실행: 155/155 성공.
- `git diff --check`: 성공.

기존 다른 게임의 작업 파일은 되돌리지 않았다. 신규 dependency는 추가하지 않았다. 검사용 독립 서버·참가자·브라우저 탭과 모바일 viewport 설정은 종료·복원했다.

## 기본판 보완 — 2026-09-16

- 교역소 보충 시점·종류별 빈자리·유물 보충 방향, 사원 타일 연구 할인을 수정했다. `research` 경로에서 일반 연구와 카드 할인을 함께 처리한다.
- `assistantSupply` projection에는 각 더미의 맨 위 한 장만 보낸다. 기존 배열 계약을 유지하며 전체 더미는 domain state에만 둔다.
- 클라이언트는 선택된 offer의 지불 카드를 손패와 확정 패널에 표시한다. 서버가 확정한 연속 revision에서만 자원 증감을 알린다. 재접속·누락 revision에는 지난 효과음과 자원 변화를 재생하지 않는다.
- 카드 종류 배지·키보드 포커스·선택 강조와 짧은 자원 알림을 보완했다. 모션 감소 설정에서는 알림 애니메이션을 생략한다.
- 추가 회귀 검증: 교역소 보충과 덱 소진, 유물 중간 효과, 사원 할인·수첩 제한, 조수 비공개, 보관 카드·구매 카드 순서·조수 재준비, 최종 수호자 공포·점수, 재접속 효과음·자원 알림.

### 보완 검증 기록

- 루트 타입 검사·빌드 성공. 통합 프런트엔드의 500kB 초과 경고는 유지된다.
- 공유 계약 133건, 웹 899건, 서버 3,858건 통과. 이전 기록의 Ark Nova 시작 실패 3건은 이번 전체 실행에서 재현되지 않았다.
- 아르낙 전용 검증은 서버 domain 14건·Socket.IO integration 1건·웹 4건으로 총 19건 통과했다. 포트 바인딩이 제한된 환경에서는 통합 테스트가 `EPERM`으로 실패하여, 로컬 소켓 허용 환경에서 루트 테스트를 다시 실행했다.
- 실제 브라우저에서 공포 카드 지불 강조·확정 후 탐험가/손패 감소·금화 +2 알림, 모바일 자유 행동 후 금화 +1 알림을 확인했다. 390×844 viewport에서 문서 폭과 scrollWidth가 모두 375px이고 브라우저 오류 로그는 없었다.
- 검증용 서버·봇·브라우저를 종료하고 viewport를 복원했다. 기존 `HomeScreen.tsx` 변경은 이번 작업에서 수정하지 않았다.
- 루트 `npm test` 최종 성공: 마지막 Space Crew E2E 155/155를 포함해 전체 5,045건 통과, 실패 0건. `git diff --check` 및 문서 로컬 링크 확인도 통과했다.

## 카드와 능력 설명 보완 — 2026-09-16

- `effect-description.ts`에서 공유 카탈로그의 효과를 화면 문구로 변환한다. 구매 할인·카드 배치 위치·복사하는 조수 등급·장소 제한·선택형 보상·비용 뒤 보상을 구체적으로 표시한다. 게임 판정은 계속 서버 domain에서 수행한다.
- 선택 카드에 일러스트·영문명·이동·획득 비용·효과·패스 대안·자가 제거 안내를 담은 상세 창을 추가했다. native modal dialog로 배경 조작을 막고 Escape·닫기 후 원래 버튼으로 초점을 돌려준다. 상세 창에는 상태 변경 행동이 없다.
- 자신의 조수와 극복한 수호자는 능력·등급·준비 여부를 확인할 수 있다. 공급 더미 맨 위 조수의 은색·금색 능력을 미리 볼 수 있다. 덱이나 공급 더미 아래 정보는 사용하지 않는다.
- 별도 라이브러리·이미지 파일 추가 없이 기존 그림의 큰 보기를 제공한다. 카드별 고유 일러스트 제작과 확장 구현은 남아 있다.

### 카드 설명 검증 기록

- 최종 키보드 초점 수정 후 분리 검증본의 루트 타입 검사·빌드를 다시 통과했다. 아르낙 domain 14건·Socket.IO integration 1건·웹 8건, 총 23건이 통과했고 `git diff --check`도 통과했다.
- 공용 작업 폴더의 루트 검증을 실행했다. 공유 계약 133건·웹 903건·서버 3,858건이 통과했다. 빌드와 마지막 E2E는 동시에 개발 중인 Terraforming Mars의 공유 타입·모듈 누락으로 실패했다. 해당 작업 파일은 변경하거나 되돌리지 않았다.
- HEAD에 아르낙 변경만 반영한 임시 검증본에서 루트 타입 검사·빌드와 Space Crew E2E 155건이 통과했다. 기존 프런트엔드 번들 500kB 초과 경고는 남아 있다. 이는 공용 작업 폴더 전체의 최종 통합 성공을 의미하지 않는다.
- 실제 브라우저에서 카드 확대·Tab 초점 유지·Escape 닫기 후 원래 버튼으로 초점 복귀·공급 조수 등급별 능력을 확인했다. 390×844 모바일에서 상세 창 폭 337px, 문서 scrollWidth 375px로 가로 넘침 없이 표시됐다.
- 검증용 서버·봇·브라우저를 종료하고 모바일 viewport를 복원했다.

## 효과음과 차례 알림 보완 — 2026-09-16

- `sound-engine.ts`는 카드의 종이 질감·발굴 저음·구매 금속음·연구와 발견의 울림을 Web Audio로 합성한다. 한 번에 한 효과음을 재생하며 종료·취소 시 오디오 노드를 해제한다. 별도 음원·dependency는 추가하지 않았다.
- 공통 출력 음량을 사용해 재생 중 음량 변경을 적용한다. 음소거와 숨겨진 탭 전환은 현재 소리를 중단한다. 숨겨진 동안의 효과음을 나중에 재생하지 않는다. 같은 화면에서 음소거를 해제하면 직전 음량으로 돌아간다.
- 소리 패널에서 카드·발굴·구매·발견을 미리 들을 수 있다. 음소거 시 미리 듣기를 비활성화하고 화면에 이유를 표시한다.
- 서버의 연속 revision에서만 내 차례·라운드 시작·라운드 정리를 자원 변화 알림과 함께 표시한다. 최초 접속·재접속·누락 revision에는 과거 알림을 표시하지 않는다. 게임 판정과 공용 계약은 변경하지 않았다.
- 브라우저 OfflineAudioContext에서 효과음 10종의 실제 파형을 렌더링했다. 모두 비영 출력이며 최대 절댓값 0.344 미만으로 클리핑이 없었다. 음소거 출력·중복 취소 후 출력은 모두 0이었다. 이는 청취 평가를 대신하지 않는다.
- 실제 소리 UI에서 음량 99 → 음소거 → 99 복원과 미리 듣기 비활성화를 확인했다. 검증 후 음량을 30으로 복원했다. 390px viewport에서 문서 폭 375px, 소리 패널이 화면 안에 표시됨을 확인하고 임시 화면을 종료했다.

### 사운드 보완 검증 기록

- 공용 작업 폴더에서 루트 `typecheck`, `test`, `build`를 모두 실행했다. 공유 계약 테스트의 이벤트 이름 일치 타입과 동시 개발 중인 `MarsScreen.tsx`의 선택적 속성 타입 오류로 실패했다. 아르낙 외 작업 파일은 변경하지 않았다.
- 아르낙 변경만 반영한 분리 검증본에서 루트 타입 검사·빌드가 통과했다. 기존 500kB 번들 경고가 유지된다. 공유 133건·웹 906건·서버 3,858건이 통과했다.
- 신규 검증은 차례·라운드 전환 및 재접속 중복 억제, 효과음 envelope 범위·질감 구분·음량 정규화, 음소거 UI이다. 기존 아르낙 규칙 테스트를 유지했다.
- 마지막 Space Crew E2E 155건도 통과하여 분리 검증본 루트 `npm test` 전체 5,052건 성공, 실패 0건이다. `git diff --check`가 통과했다. 공용 작업 폴더 전체의 통합 검증은 앞서 기록한 다른 작업의 타입 오류가 해결된 뒤 다시 필요하다.

## 카드별 일러스트 — 2026-09-16

- 카드 80개 정의(아이템 40·유물 35·시작 카드 4·공포 1)에 각각 다른 신규 그림을 연결했다. `card-art.ts`에 카드 ID와 이미지 묶음의 위치를 명시해 카탈로그 정렬 순서에 영향을 받지 않는다. 서버 규칙·공유 계약은 변경하지 않았다.
- `ArnakCardArt`를 손패·교역소·공개 카드·상세 창에서 공통 사용한다. 짧은 카드에는 정사각 그림의 가운데를 표시하고, 확대창에는 정사각형을 표시한다. 공포 그림의 기존 색상 변환은 새 어두운 그림에 중복 적용하지 않는다.
- 생성 도구는 내장 image_gen이며 정확한 프롬프트와 원본 이름을 이미지 폴더의 `card-art-prompts.json`에 기록했다. WebP 5개 총 약 3.5MB를 추가했다. `atlas.webp`는 장소·수호자 등 기존 용도로 유지한다.
- 생성 결과의 행 경계가 일부 불균등하여 실측 경계와 안쪽 여백을 적용했다. 모바일 오카리나 확대창에서 다음 행 그림이 섞이던 현상을 수정하고 재확인했다.
- 카드 ID 전체 포함·중복 없는 80개 이미지 위치·파일 존재 및 용량·작은 카드와 확대창의 동일 소스·접근성 라벨을 검사한다. 데스크톱 장화와 모바일 오카리나 상세를 실제 브라우저에서 확인했으며 390px viewport의 문서 폭은 375px이다.

### 카드 그림 검증 기록

- 공용 작업 폴더에서 루트 `npm run typecheck`와 `npm run build`가 통과했다. 이미지 경계 보정 후 두 검증을 다시 통과했고 웹 테스트 910건도 재통과했다. 기존 통합 프런트엔드 번들의 500kB 초과 경고는 남아 있다.
- 브라우저 렌더링에서 80개 그림과 중복 없는 80개 위치를 확인했다. 오류 로그는 없었다. 임시 카드 갤러리는 제품에 추가하지 않았으며 검증 서버·탭을 종료하고 viewport를 복원했다.
- 남은 시각 작업은 장소·수호자·조수의 개별 일러스트 보완이다. 선택 확장판은 미구현 상태이며 이번 작업은 기본판 카드 그림만 변경한다.
- 공용 작업 폴더의 루트 `npm test` 최종 성공: 공유 133·웹 910·서버 3,878·Space Crew E2E 155, 총 5,076건 통과했다. 이전 사운드 작업 때의 다른 작업 타입 오류는 이번 실행에서 재현되지 않았다. `git diff --check`도 통과했다.

## 장소·수호자·조수 일러스트 — 2026-09-16

- 장소 21곳·수호자 15종·조수 12명에 각각 다른 그림을 연결했다. `world-art.ts`의 명시적 ID 매핑과 `ArnakWorldArt`를 지도·보유 능력·공급 조수·설명 패널에서 재사용한다.
- 설명 패널에 선택된 공개 대상의 큰 그림을 추가했다. 조수의 56px 정사각 초상화는 은색/금색과 준비 상태 텍스트를 유지한다. 기존 카드 그림과 서버 판정·계약은 변경하지 않았다.
- 미발견 장소의 뒷면은 그대로 유지한다. `null`·`undefined`·알 수 없는 ID는 그림을 출력하지 않는다. 조수 공급 더미는 기존 맨 위 공개 항목만 사용한다.
- 내장 image_gen으로 4×4 이미지 묶음 3개를 만들고 WebP로 인코딩했다. 추가 용량 1,875,336바이트이며 생성 프롬프트·원본 이름은 `world-art-prompts.json`에 기록했다. 이미지 묶음의 가장자리 여백을 두어 이웃 그림이 섞이지 않게 한다.
- 브라우저에서 장소·수호자·조수 48개 고유 위치, 항공 조종사 초상화, 숲속 서고·황금 전갈 선택과 확대 그림을 확인했다. 모바일 390px에서 문서 폭 375px, 오류 로그 0건이며 검증 탭·서버를 종료하고 viewport를 복원했다.
- 카탈로그 48개 전체 포함·고유 그림·파일 존재/용량·비공개 ID 무표시·작은 그림과 설명 그림의 같은 소스를 회귀 검사한다.

### 세계 그림 검증 기록

- 공용 저장소 루트 `npm run typecheck`·`npm run build` 통과. 기존 통합 프런트엔드 번들 500kB 초과 경고는 유지된다.
- 공유 계약 133건·웹 912건·서버 3,878건 통과. 별도 dependency 추가나 공용 계약 변경은 없다. `git diff --check` 통과.
- 마지막 Space Crew E2E 155건도 통과하여 루트 `npm test` 전체 5,078건 성공, 실패 0건으로 완료했다.

## 상대 탐험대 공개 정보 — 2026-09-16

- `ArnakOpponentCamp`는 `ArnakPlayerPublic`만 받아 상대의 손패/덱 장수, 남은 탐험가, 돋보기·수첩 위치, 우상 슬롯, 사원 타일 점수와 공포 대체 타일을 표시한다. 전체 점수를 추정하지 않는다.
- 상대가 고용한 조수의 현재 등급 능력·준비 상태, 극복한 수호자의 축복·사용 상태를 그림과 함께 펼쳐 볼 수 있다. 행동을 실행하는 command는 연결하지 않는다.
- 사용한 공개 카드는 그림 목록으로 표시하고 기존 카드 상세창을 연다. 상세 대상 목록에 각 플레이어의 `played`만 추가했으며, 상대 손패·덱 내용은 읽지 않는다. 다음 라운드에 공개 목록에서 사라진 카드는 상세창에서도 사라진다.
- 부모 탐험대와 하위 능력 펼침이 서로 다른 스타일을 사용하도록 범위를 좁혔다. 모바일에서는 공개 카드 목록 안에서만 가로 스크롤한다.
- 실제 브라우저에서 금색 조수의 금화 3 효과, 수호자의 배 이동 효과, 상대 카드 확대·Escape 닫기 후 카드 버튼으로 초점 복귀를 확인했다. 390px viewport에서 문서 폭 375px, 카드 목록 폭 313px/내용 폭 604px, 오류 로그 0건이었다. 임시 검증 서버·탭을 종료하고 viewport를 복원했다.
- 빈 탐험대 안내와 공개 상태 표시, 추가로 주어진 비공개 필드가 HTML에 나오지 않음을 테스트했다. 서버 규칙·공유 DTO·dependency 변경은 없다.

### 상대 탐험대 검증 기록

- 루트 `npm run typecheck`·`npm run build` 통과. 기존 통합 프런트엔드 번들 500kB 초과 경고는 남아 있다.
- 공유 계약 133건·웹 917건·서버 3,885건 통과. 아르낙 외 동시 작업 파일은 변경하거나 커밋에 포함하지 않는다.
- 마지막 E2E 155건까지 루트 `npm test` 총 5,090건 통과, 실패 0건. `git diff --check`도 통과했다.

## 지불 방법 선택 UI — 2026-09-16

- `ArnakOfferList`와 순수 표시 함수 `groupArnakOffers`로 같은 행동의 지불 방법을 묶는다. 서버가 준 offer ID·카드 인스턴스·비용·순서를 그대로 유지하며 새로운 지불 방법을 계산하지 않는다.
- 사용자는 아끼려는 손패를 선택해 해당 카드 인스턴스를 소비하는 방법을 숨길 수 있다. 같은 이름의 카드는 손패 위치로 구분한다. 필터 변경·초기화 시 기존 선택을 해제하고, 대상 또는 gameRevision 변경 시 필터도 초기화한다.
- 필터 결과 수와 결과 없음 안내·복원 버튼을 제공한다. 자원 지불과 카드 지불을 줄을 나눠 표시한다. 실제 행동은 기존 선택 확정 단계와 서버 검증을 거치며 선택 효과음 연결도 유지한다.
- 그룹 분리, 순서와 ID 보존, 카드 인스턴스별 필터, 빈 결과, SSR 선택 표시를 테스트한다. 임시 브라우저 화면에서 선택 해제·필터 복원·revision 변경 초기화를 확인했다. 390px viewport에서 문서 폭과 스크롤 폭이 모두 375px였고 브라우저 오류 로그는 없었다. 임시 서버·탭 종료 및 viewport 복원 완료.
- 서버 규칙·공유 DTO·dependency 변경은 없다. 선택 확장판 등 기존 미확정 사항은 이번 작업에서 확정하지 않는다.
- 공용 저장소 루트 typecheck·test·build는 실행 당시 동시 작업 중인 `ArkNovaBoard.tsx` 문법 오류로 실패했다. 해당 파일은 변경하지 않고 HEAD에 이번 아르낙 변경만 적용한 임시 소스로 별도 검증한다.
- 분리 소스에서 루트 typecheck·build·test 통과: 공유 133·웹 918·서버 3,878·E2E 155, 총 5,084건 실패 0건이다. 최종 비용 문구·레이아웃 보정 후 typecheck·build도 재통과했다. 기존 통합 프런트엔드 번들 500kB 초과 경고는 남아 있다.
- 최종 비용 문구를 포함한 웹 회귀 테스트 918건도 재통과했다. `git diff --check` 통과.

## 연구·사원 미리 보기 — 2026-09-16

- `ArnakResearchDetail`은 연구 칸 선택 시 공유 카탈로그의 기본 비용·연결된 이전 칸·돋보기/수첩의 서로 다른 보상을 표시한다. 출발점과 사원 도착 칸은 일반 단계 보상 대신 별도 안내를 제공한다.
- 사원 선택 시 6종 타일의 점수·기본 비용·서버가 공개한 잔여 수량을 표시한다. 소진된 타일도 0개로 표시한다. 실제 이동 가능 여부와 할인된 비용은 기존 서버 offer 목록으로 확인하고 확정한다.
- 새 화면은 조회만 제공한다. 서버 규칙·공유 계약·의존성은 변경하지 않았다. 기존 선택 효과음과 모바일 행동 패널 이동을 재사용한다. 확장판·공식 한국어 용어 대조는 기존 후속 범위로 남긴다.
- 연구 보상/경로, 출발·도착 예외, 알 수 없는 대상, 사원 잔여 수량을 회귀 검사한다. 임시 브라우저 화면에서 연구/사원 전환과 390×844 모바일을 확인했고 문서 가로 넘침과 오류 로그가 없었다. 검증용 파일·서버·탭을 제거하고 viewport를 복원했다.
- 루트 `npm run typecheck`·`npm run build` 통과. 기존 500kB 번들 경고는 유지된다. 루트 `npm test`에서 공유 133·웹 931건 통과, 서버는 3,924건 중 3,923건 통과했다. 실패한 SNEAKY 8인 실시간 부하 테스트는 `Too little exercise: 120`이며 아르낙 실패는 없다. 원래 전체 실행의 실패를 유지해 기록하고 해당 테스트를 단독 재실행한다.
- 실패했던 SNEAKY 테스트는 동일 코드의 단독 실행에서 통과했다(다른 테스트는 이름 필터로 제외). 전체 동시 실행의 불안정성은 남아 있다. 앞선 실패로 미실행된 Space Crew E2E는 별도 실행하여 155건 모두 통과했다. `git diff --check` 통과.

## 라운드 정리 손패 선택 — 2026-09-16

- 자신의 정리 차례에 `ArnakCleanup` 패널을 표시한다. 서버 projection의 hand/keep을 기준으로 보관할 손패와 내려놓을 손패를 그림·장수로 구분하며 같은 이름의 카드는 원래 손패 번호로 식별한다.
- 카드 클릭은 기존 대상 선택·효과음·확정 흐름을 재사용한다. 클릭만으로 보관 상태를 바꾸지 않으며 확정된 keep만 목록에 반영한다. 연결 끊김·명령 처리 중에는 선택 버튼을 비활성화한다.
- 정리 단계의 패스 확정 패널에 보관/내려놓기 장수를 표시한다. 다른 플레이어의 정리 차례나 종료된 게임에는 본인 정리 패널을 표시하지 않는다. 서버 규칙·공유 DTO·dependency 변경은 없다.
- 회귀 테스트는 동일 이름 카드의 인스턴스 분리, 손패 번호 유지, 알 수 없는 keep 참조 무시, 빈 그룹과 비활성화를 검사한다. 브라우저에서 카드 선택 후 확정 보관 목록이 유지됨을 확인했고 390px 모바일 가로 넘침·오류 로그는 없었다. 임시 검증 파일·서버·탭을 정리했다.
- 기존 미확정 사항과 선택 확장판은 변경하지 않았다.
- 루트 타입 검사·빌드 통과. 기존 번들 크기 경고는 변화 없이 유지된다. 공유 133건·웹 933건 통과했으며 `git diff --check`도 통과했다.
- 루트 `npm test` 최종 통과: 공유 133·웹 933·서버 3,924·E2E 155, 총 5,145건 실패 0건. 이전 SNEAKY 실시간 부하 실패는 이번 실행에서 재현되지 않았다.
- 최종 아르낙 범위의 `git diff --check`는 통과했다. 저장소 전체 검사는 검증 도중 추가된 다른 작업의 `apps/server/src/application/turn-transition.ts` 244–245행 공백 때문에 실패했으며 해당 파일은 수정하지 않았다. 위 결과는 각 검증 실행 시점의 소스 기준이다.

## 가능한 행동 찾기 — 2026-09-16

- `ArnakActionBrowser`는 내 일반 행동 차례의 전체 탐험 수첩에 발굴·발견·구매·연구·수호자·카드 사용 목록을 제공한다. 추가 효과 선택·라운드 정리·게임 종료 중에는 표시하지 않는다. 기존 우상·조수·축복·차례 종료/패스 목록은 유지한다.
- 순수 표시 함수 `arnakActionTargets`가 서버 offer를 종류/대상별로 묶는다. 같은 대상의 여러 지불 선택지는 대상 수를 늘리지 않는다. 클릭은 기존 대상 선택·효과음·모바일 패널 이동을 재사용하며 실제 명령을 보내지 않는다.
- 지도와 목록에서 미발견 장소의 동일한 공개 슬롯 번호를 표시한다. 대상 이름은 이미 공개된 카드/장소/연구 칸에서 가져오며 서버 offer에 없는 대상은 목록에 출력하지 않는다. 서버 규칙·DTO·dependency 변경은 없다.
- 테스트는 지불 대안 중복 집계 방지, 차례 제어 제외, 공개 이름 사용과 미제시 대상 무표시, 차단 상태 및 빈 목록을 검사한다. 임시 브라우저 화면에서 펼침과 대상 선택, 390px 모바일 가로 넘침 없음과 오류 로그 0건을 확인했다. 임시 파일·서버·탭을 정리하고 viewport를 복원했다.
- 확장판과 공식 용어 대조는 기존 후속 범위로 남긴다.
- 루트 `npm run typecheck`·`npm run build`·`npm test` 통과. 공유 133·웹 938·서버 3,939·E2E 155, 총 5,165건 실패 0건이다. 기존 번들 크기 경고는 유지되며 `git diff --check`도 통과했다.

## 탐험 기록 탐색 — 2026-09-16

- `ArnakJournal`로 기존 최근 15건 목록을 교체했다. 라운드·탐험가 조건을 함께 적용하고, 최신순으로 15건씩 이전 기록을 더 볼 수 있다. 필터 변경이나 초기화 시 표시 개수도 15건으로 돌아간다.
- 서버가 이미 공개한 history만 사용한다. 최근 최대 120건이라는 기존 보관 한계를 안내하며 전체 대국 기록이나 비공개 카드 정보를 새로 저장하지 않는다. 표시 함수는 원본 history 순서를 변경하지 않는다.
- 빈 기록·조건 불일치 안내와 표시 개수를 제공한다. native select에 라벨과 키보드 초점을 연결하고 기록 문자열은 React 텍스트로 렌더링한다. 서버 규칙·계약·dependency 변경은 없다.
- 필터 조합·최신순·원본 보존·첫 15건·이전 기록 접근·빈 기록·HTML 이스케이프를 테스트한다. 브라우저에서 15→22건 펼침, 2라운드/앨리스의 빈 결과, 초기화 후 15건 복원, 모바일 가로 넘침 없음·오류 로그 0건을 확인했다. 임시 검증 파일·서버·탭을 정리하고 viewport를 복원했다.
- 확장판과 공식 용어 대조는 기존 후속 범위로 남긴다.
- 루트 typecheck·build·test 통과: 공유 133·웹 941·서버 3,940·E2E 155, 총 5,169건 실패 0건. 기존 번들 경고는 유지되고 아르낙 범위 `git diff --check`는 통과했다.

## 탐험 결과 비교 — 2026-09-16

- `ArnakResults`에서 서버가 확정한 총점·승자 목록을 표시한다. 승자 배지와 본인 표시를 구분하고, 동점 총점만으로 승자를 추정하거나 점수를 다시 계산하지 않는다. 여러 승자도 그대로 표시한다.
- 연구·사원·수호자·우상·미사용 슬롯·카드·공포와 총점을 표로 비교할 수 있다. 공포는 음수로 표시한다. 기존 캠프 일러스트를 배경으로 재사용하고 좁은 화면에서는 표 내부만 가로 스크롤한다.
- 중단된 탐험에는 승리 배지와 최종 점수표를 표시하지 않는다. 방장의 재경기 준비 버튼과 연결/처리 중 비활성화를 유지하며 다른 참가자에게 방장 준비 안내를 표시한다. 기존 종료 효과음·서버 규칙·공유 DTO는 변경하지 않는다.
- 서버의 동점 승자·공동 승리·중단·재경기 비활성화를 회귀 검사했다. 브라우저에서 4인 점수 비교, 공포 감점과 본인 표시를 확인했다. 모바일 문서 폭 375px, 표 컨테이너 271px/내용 420px로 문서 가로 넘침과 오류 로그는 없었다. 임시 검증 파일·서버·탭을 정리하고 viewport를 복원했다.
- 확장판·공식 용어 대조 등 기존 후속 범위는 변경하지 않는다.
- 초기 웹 타입 검사에서 승자 ID 비교 타입 오류 1건을 발견해 수정했다. 수정 후 루트 typecheck·build·test 통과: 공유 137·웹 946·서버 3,941·E2E 155, 총 5,179건 실패 0건. 기존 번들 경고는 유지되고 아르낙 범위 `git diff --check`도 통과했다.

## 실제 브라우저 대국 회귀 — 2026-09-16

- `e2e/arnak.browser.test.mjs`와 `npm run test:arnak-browser`를 추가했다. 기존 로컬 Playwright 방식과 설치된 Chrome을 재사용하며 dependency 추가는 없다. 실행 방법과 한계는 [ARNAK_BROWSER_TEST.md](ARNAK_BROWSER_TEST.md)에 기록했다.
- 실제 UI로 방 생성·참가·확대창·카드 사용·발굴·보관·새로고침 복구·5라운드 결과 비교·재경기를 검증한다. 양쪽 브라우저가 실제 발굴을 수행해야 하며 보관 카드 복원은 이름뿐 아니라 손패 위치를 포함한 인스턴스 식별로 확인한다. 효과음은 실제 오실레이터 시작을 관찰하고 음소거 상태를 확인한다.
- 첫 실행과 보관/효과음 검증 강화 후 실행이 모두 통과했다. 최종 실행은 데스크톱/모바일이 49회 UI 명령으로 5라운드를 완료하고 재경기를 시작했으며 약 11초가 걸렸다. 결과 스크린샷과 모바일 문서 가로 넘침 없음·pageerror 0건을 확인했다.
- 루트 타입 검사·빌드는 통과했다. 기존 번들 경고는 유지된다. 루트 test는 공유 137건 통과, 웹 948건 중 947건 통과/1건 실패였다. 실패는 동시 개발 중인 DUEL reference chain 테스트이며 해당 게임 파일은 수정하지 않았다. 아르낙 브라우저 검증은 위와 같이 별도로 통과했다.
- 웹 실패 뒤 서버 테스트를 별도 실행했으나 동시 개발 중인 Mars `domain/game.ts`의 `marsCorporation` 미사용 import(TS6133) 때문에 컴파일에서 중단됐다. 후속 Space Crew E2E는 이 실행에서 시작되지 않았다. 공용 저장소 전체 검증 성공으로 간주하지 않는다. 일회성 아르낙 서버와 브라우저는 종료했으며 변경 파일의 `git diff --check`와 테스트 스크립트 구문 검사가 통과했다.

## 연구·구매 브라우저 회귀 확장 — 2026-09-16

- 재경기 후 실제 시작 카드와 기본 장소로 자원을 확보하고 1L 돋보기 연구와 아이템 구입까지 수행한다. 첫 플레이어의 초기 금화와 손패 구성에 따라 항상 가능한 경로를 사용하며 난수·서버 상태·응답을 주입하지 않는다.
- 연구 안내 비용/보상, 나침반·화살촉 차감, 금화 보상, 두 참가자 화면의 말 이동과 새로고침 복원을 검사한다. 아이템은 표시 가격만큼 차감하고 손패 대신 덱이 증가해야 하며 구매 직후와 차례 종료 후의 교역소 상태를 양쪽 화면에서 확인한다. 구매 직후 새로고침 복원도 포함한다.
- 브라우저 확장 실행과 구매 복원 검사 추가 후 최종 실행이 모두 통과했다. 최종 실행 약 14초, 기존 5라운드 49회 명령 후 재경기에서 연구·구매까지 검증했으며 pageerror는 0건이다. 결과 및 연구·구매 후 데스크톱/모바일 스크린샷을 남겼다.
- 기본 규칙·게임 계약·dependency 변경은 없다. 유적 발견·수호자 극복·유물 구입·고급 연구 조합의 브라우저 자동 검증은 후속 범위로 남는다.
- 루트 `npm run typecheck`·`npm run build`·`npm test` 통과. 공유 139·웹 949·서버 3,950·E2E 155, 총 5,193건 실패 0건이며 아르낙 브라우저 검사도 별도로 통과했다. 기존 번들 크기 경고는 유지된다. 테스트 스크립트 구문 검사와 `git diff --check`를 통과했고 일회성 브라우저/서버를 종료했다.

## 유적 발견·미극복 수호자 브라우저 회귀 — 2026-09-16

- 재경기 2라운드에서 다른 참가자가 시작 카드와 탐험가의 길로 나침반을 확보하고 1단계 유적을 발견한다. 지도에서 실제 가능한 장소를 선택하며 우상 획득, 탐험가 배치, 공개된 발견 비용을 확인한다.
- 무작위 장소·우상 보상에 추가 선택이 발생하면 각 단계에서 새로고침하고 동일한 대기 단계가 복원된 뒤 UI로 처리한다. 최대 12회로 제한해 선택이 끝나지 않는 오류를 놓치지 않는다. 두 참가자 화면의 장소·수호자 이름과 극복 비용 안내가 일치해야 한다.
- 수호자를 극복하지 않고 라운드를 마칠 때 다른 카드 획득·제거 없이 총 보유 카드 수가 정확히 1장 증가하는지 검사한다. 탐험가 복귀와 지도에 남은 수호자도 양쪽 화면에서 확인한다. 비공개 덱 내용을 읽거나 서버 상태를 주입하지 않는다.
- 3회 브라우저 실행이 통과했고 추가 보상 2단계/1단계 사례를 확인했다. 최종 실행 약 22초, pageerror 0건이며 모바일 가로 넘침 검사도 통과했다. 유적 발견 및 수호자 안내 스크린샷을 추가했다. 일회성 서버와 브라우저를 종료했다.
- 수정 대상은 기존 브라우저 테스트와 관련 문서다. 게임 규칙·DTO·dependency 변경은 없으며 수호자 극복·유물 구매·고급 연구의 브라우저 회귀는 후속 범위다.
- 루트 타입 검사·빌드·전체 테스트 통과: 공유 140·웹 953·서버 3,955·E2E 155, 총 5,203건 실패 0건. 기존 번들 크기 경고는 유지된다. 아르낙 범위 `git diff --check`와 테스트 스크립트 구문 검사도 통과했다.

## 수호자 극복·유물 구매 브라우저 회귀 — 2026-09-16

- 기존 UI 대국을 이어 공개된 수호자 비용을 읽고 우상·기본 장소로 자원을 준비한다. 유적 재방문 후 확정 패널의 비용·선택된 지불 카드 수와 실제 자원/손패 감소를 대조한다. 양쪽 지도에서 수호자가 사라지고 본인 야영지에 5점·축복 사용 가능 상태로 이동하는지 확인한다.
- 새로고침 후 소유 상태를 확인하고 축복을 사용한다. 후속 선택 처리 뒤 사용 완료·5점 유지, 다음 라운드의 공포 미추가를 검사한다. 최대 두 라운드 동안 나침반을 모아 실제 유물을 구매하고 표시 가격·확정 비용, 즉시 사용한 카드 더미 편입, 새로고침 후 효과 처리와 교역소 보충을 확인한다.
- 최초 실행 및 새로운 무작위 대국 3회, 총 4회 통과했다. 가시 멧돼지·붉은 호랑이·뿔 도마뱀과 죽은 자의 룬·길잡이 돌·오래된 포도주·코코넛 물병을 확인했다. 실행은 약 23~28초, pageerror 0건이며 데스크톱/모바일 스크린샷을 남겼다. 모든 수호자·유물 조합을 검증했다는 의미는 아니다.
- 변경은 브라우저 테스트와 문서에 한정된다. 서버 상태·난수 주입, 계약·규칙·dependency 변경은 없다. 고급 연구·3~4인 브라우저 대국·패킷 유실 검증은 후속 범위로 남는다.
- 단순 자동 효과 유물의 인쇄 문구와 자원·총 카드 수를 대조하는 조건부 검사를 추가한 최종 실행도 통과했다(약 21초). 황금 전갈의 손패 버리기 비용과 아라아누의 우상 후속 선택을 확인했다. 최종 대국의 유물은 복합 효과여서 단순 자동 효과 대조 분기는 이 실행에서 수행되지 않았다. 합계 5회 브라우저 검증이며 무작위 구성에 따른 개별 분기 커버리지 한계는 유지된다.
- 최초 타입 검사는 동시 변경 중이던 Mars `nextCardDiscount` 타입 불일치로 실패했다. 최신 공유 타입 빌드 후 루트 타입 검사 재실행·빌드·전체 테스트는 통과했다: 공유 140·웹 954·서버 3,958·E2E 155, 총 5,207건 실패 0건. 해당 Mars 코드는 수정하지 않았다. 기존 번들 크기 경고는 유지되며 아르낙 범위 공백 검사와 테스트 스크립트 구문 검사도 통과했다.

## 3~4인 브라우저 대국 회귀 — 2026-09-16

- 기존 2인 테스트에 더해 독립 브라우저 context 3개/4개로 방 생성·참가·시작부터 5라운드 종료까지 검증한다. 마지막 참가자는 390px 모바일 화면을 사용한다. 매 참가자가 자금 지원과 매 라운드 기본 장소 발굴·패스를 수행하며 3인은 63회, 4인은 84회 실제 UI 명령을 확정한다.
- 인원별 기본 장소 차단(3인 3개, 4인 0개), 상대 캠프 수, 차례 순서, 정리 중 차례 유지, 라운드마다 선 플레이어 교대를 확인한다. 2라운드의 비활성 참가자 새로고침 후 손패·자원·현재 차례가 유지되어야 한다.
- 모든 참가자의 승자·점수표 일치, 결과 카드 수, 게스트 재경기 버튼 무표시를 확인한다. 데스크톱/모바일 결과 스크린샷과 문서 가로 넘침 검사, pageerror 검사도 포함한다. 새 다인 테스트 2건의 첫 실행은 약 28초에 통과했다.
- 서버 계약·게임 규칙·dependency 변경은 없다. 고급 연구, 다인 복합 카드 조합, 패킷 유실/오프라인 복구는 후속 범위다.
- 기존 2인까지 포함한 전체 브라우저 실행 3건이 약 45초에 모두 통과했다(실패·스킵 0). 모바일 4인 결과 화면을 확인했고 일회성 서버와 모든 브라우저를 종료했다. 테스트 스크립트 구문 검사와 아르낙 변경 파일의 `git diff --check`도 통과했다.
- 루트 타입 검사는 통과했다. 뒤이은 빌드는 동시 수정 중인 DUEL `domain/game.ts`·`setup.ts`의 `turnDurationSeconds` 타입 불일치로 실패했다. 루트 test는 공유 140건 통과 후 DUEL UI 테스트 68행의 동일 필드 누락(TS2769)으로 웹 컴파일에서 중단됐다. 웹 테스트·서버 테스트·후속 Space Crew E2E는 해당 실행에서 시작되지 않았다. DUEL 코드는 수정하지 않았으며 전체 저장소 검증 성공으로 간주하지 않는다. 웹 빌드의 기존 번들 크기 경고도 유지된다. 브라우저 검증은 아르낙 코드 변경이 없는 기존 서버 빌드와 로컬 실제 UI를 사용했다.

## 연구 수첩·조수 고용 브라우저 회귀 — 2026-09-16

- 다인 시나리오의 모바일 참가자가 2라운드에 돋보기, 3라운드에 수첩을 1L로 이동한다. 실제 탐험 카드·고대 제단으로 나침반·화살촉을 확보하고 비용 차감과 전원 화면의 연구 말 위치를 확인한다.
- 수첩 보상의 조수 선택 3개가 새로고침 후 그대로 복원되고, 고용 전에는 차례 종료가 제시되지 않아야 한다. 고용하면 공급 더미에서 해당 조수가 사라지고 은색·준비 상태로 본인 캠프와 모든 상대의 공개 캠프에 나타나는지 검사한다. 고용 이후 새로고침 복원과 연구 참가자의 최종 승리도 확인한다.
- 추가 행동을 포함해 3인은 70회, 4인은 91회 UI 명령을 사용한다. 신규 연구/고용 흐름의 첫 3·4인 실행 2건은 약 30초에 통과했고 모바일 조수 고용 화면을 확인했다. 난수·서버 상태 주입, 게임 규칙·공유 계약·dependency 변경은 없다.
- 높은 단계의 연구·조수 승급·사원 도착과 모든 조수 능력 조합은 후속 검증 범위로 남는다.
- 최종 2·3·4인 브라우저 실행 3건은 약 48초에 전부 통과했다(실패·스킵·pageerror 0). 아르낙 게임 코드를 변경하지 않은 기존 서버/웹 빌드로 검증했고 일회성 서버와 브라우저를 종료했다. 구문 검사와 아르낙 변경 파일 공백 검사도 통과했다.
- 루트 타입 검사와 test는 동시 변경 중인 공유 `contract.test.ts` 4625행 이벤트 집합 검사(TS2322) 때문에 중단됐다. 루트 build는 미완성 `features/great-kingdom/GreatKingdomScreen.js` import(TS2307) 때문에 웹 타입 검사에서 중단됐다. 해당 파일들은 수정하지 않았으며 전체 저장소 검증 성공으로 간주하지 않는다. 공유 테스트 실행·웹/서버 테스트·Space Crew E2E는 이번 루트 test에서 시작되지 않았다.

## 야영지 조수·수호자 가독성 — 2026-09-16

- 야영지의 밝은 글자색이 밝은 버튼에 상속되어 조수 이름과 상태를 읽기 어려웠다. 기존 빌드의 실제 조수 고용 화면에서 대비 검사 실패(약 1.25:1)를 재현했다.
- 야영지 버튼 글자색을 명시하고 조수/수호자 상태 설명을 11px로 키웠다. 준비 상태는 밝은 배경과 금색 실선, 사용 완료 상태는 회색 배경과 점선으로 구분한다. 상태 문구와 키보드 초점 표시를 유지하며 카드·우상·사용한 카드 버튼에도 읽을 수 있는 글자색이 적용된다.
- 수호자 버튼에도 조수와 같은 준비/사용 완료 클래스를 연결한다. 사용 완료 후에도 상세를 살펴볼 수 있고 기존 선택·확정·효과음·점수 동작은 유지한다. 규칙·서버·DTO·dependency 변경은 없다.
- 브라우저 테스트에서 조수 준비 상태와 수호자 축복 사용 전/후의 이름·상태 설명 대비를 계산해 4.5:1 이상인지 검사한다.
- 수정 후 새 웹 빌드로 2·3·4인 브라우저 회귀 3건이 약 80초에 통과했다(실패·스킵·pageerror 0). 조수 준비 및 수호자 축복 사용 전/후 이름·상태 설명 대비가 4.5:1 이상이었고 모바일 조수 화면도 직접 확인했다. 서버/브라우저를 종료했으며 구문·공백 검사를 통과했다.
- 최초 타입 검사는 동시 개발 중인 Speakeasy `SpeakeasyGoal` 필드 접근과 테스트의 미사용 import 오류로 실패했다. 이후 루트 타입 검사 재실행·빌드·전체 테스트는 모두 통과했다. 공유 140·웹 980·서버 4,048·E2E 155, 총 5,323건 실패 0건이다. 해당 Speakeasy 파일은 수정하지 않았다. 기존 번들 크기 경고는 유지된다.

## 야영지 능력 요약 — 2026-09-16

- 보유 조수 카드에 현재 등급(은색/금색)의 능력과 자유/주 행동 구분을 표시한다. 수호자 카드에는 일회성 축복 설명을 표시하고 사용 완료 뒤에도 효과와 5점 안내를 유지한다. 기존 카탈로그와 효과 설명 함수를 사용하며 비용/실행 가능 여부 판정은 기존 서버 offer와 확정 패널에 맡긴다.
- 선택한 조수·수호자 카드에 `aria-pressed`와 선택 테두리를 연결한다. 카드는 최대 300px, 작은 화면에서는 가용 폭에 맞추고 긴 설명은 줄바꿈한다. 기존 그림·준비/사용 완료 스타일·키보드 초점·효과음·클릭 후 확정 흐름을 유지한다.
- 브라우저 검증에 카드 요약/상세 내용 일치, 선택 상태, 새로고침 후 설명 유지, 모든 상태·효과 설명의 대비, 모바일 카드 내부 넘침을 추가했다. 모바일 능력 카드 영역 스크린샷도 따로 저장한다. 조수 승급 후 금색 능력 전환의 전체 대국 검증은 후속 범위다.
- 새 웹 빌드로 2·3·4인 브라우저 회귀 3건이 약 55초에 통과했다(실패·스킵·pageerror 0). 모바일 운전사의 긴 선택형 능력과 금광 탐사자의 짧은 자원 능력 카드 이미지를 직접 확인했다. 카드 내부/문서 가로 넘침과 대비 검사도 통과했고 일회성 서버와 브라우저를 종료했다.
- 루트 타입 검사·빌드·전체 테스트 통과: 공유 142·웹 980·서버 4,051·E2E 155, 총 5,328건 실패 0건. 기존 번들 크기 경고는 유지되며 아르낙 범위 공백 검사와 브라우저 스크립트 구문 검사도 통과했다.

## 고용 조수 능력 비교 — 2026-09-16

- 기존 고용 가능한 조수 펼침 영역에서 각 후보의 은색·금색 능력과 자유/주 행동 구분을 함께 보여준다. 고용 시 은색으로 시작한다는 설명을 추가하며 공개된 공급 더미 맨 위만 사용한다. 아래 조수 순서는 노출하지 않는다.
- 선택 표시와 키보드 초점을 유지하며 데스크톱은 가용 폭에 맞춘 카드 열, 모바일은 한 열로 정렬한다. 카드 선택은 기존 상세 패널·효과음으로 연결되고 실제 고용은 기존 확정 버튼으로만 수행한다.
- 브라우저에서 조수 후보 3명의 양면 설명과 상세 패널의 일치, 비교 중 기록 불변·고용 선택지 유지, 선택 표시와 대비·모바일 내부 넘침을 검사한다. 규칙·서버·DTO·dependency 변경은 없다.
- 실제 Chrome 2·3·4인 회귀 3건이 재검증에서 약 47초에 통과했다(실패·스킵 0). 모바일 비교 카드 이미지도 직접 확인했다. 최초 실행에서는 기존 유물 검사기가 `죽은 자의 룬`의 공포 수락 전 보상을 즉시 기대해 실패했다. 공포 수락 전 비용·카드 수와 수락 후 보상을 따로 검사하도록 수정했다. 재실행의 무작위 시장에서는 다른 유물이 나왔으므로 해당 분기 재현까지 확인한 것은 아니다.
- 루트 타입 검사와 빌드는 통과했고 기존 번들 크기 경고는 유지된다. 전체 테스트는 공유 143건·웹 980건 통과 후, 동시 작업 중인 범위 밖 `apps/server/src/games/speakeasy/domain/luciano.ts`의 모듈 참조·미사용 import·암시적 any 오류로 서버 컴파일에서 중단됐다. 서버 테스트와 후속 Space Crew E2E는 실행되지 않았다. 아르낙 범위 공백 검사와 브라우저 스크립트 구문 검사는 통과했다.

## 조수 선택 목록의 능력 미리보기 — 2026-09-16

- 조수를 대상으로 하는 서버 `EFFECT` 선택지에 공개 그림, 자유/주 행동 구분, 은색·금색 능력을 함께 표시한다. 고용 보상에서 야영지로 이동하지 않고 후보를 비교할 수 있으며 준비·승급·복사 선택도 해당 조수의 카탈로그 설명을 참고할 수 있다.
- 미리보기는 읽기 전용이다. 서버가 제공한 선택지와 ID, 비용, 버튼 비활성화, 선택 후 확정·효과음 흐름을 그대로 유지한다. 클라이언트가 행동을 추가하거나 사용 가능 여부를 판정하지 않는다.
- 정적 렌더 검사로 능력 설명·버튼 수·비활성화·비조수 대상 제외를 확인한다. 3·4인 실제 브라우저 검사에서 후보 3명의 미리보기와 야영지 설명 일치, 모바일 가로 넘침을 확인하고 고용 선택지 스크린샷을 남긴다.
- 새 빌드의 Chrome 2·3·4인 회귀 3건이 약 62초에 통과했다(실패·스킵 0). 모바일 항공 조종사의 긴 선택형 능력과 고용 버튼 배치를 이미지로 확인했다. 일회성 서버와 브라우저는 종료했다.
- 루트 타입 검사·빌드·전체 테스트가 모두 통과했다: 공유 144·웹 981·서버 4,067·E2E 155, 총 5,347건 실패 0건. 기존 번들 크기 경고는 유지된다. 아르낙 범위 공백 검사와 브라우저 스크립트 구문 검사도 통과했다. 높은 단계 연구·조수 승급 후 금색 능력 사용의 전체 브라우저 대국 검증은 후속 범위다.

## 카드 이동 수단 표시 — 2026-09-16

- 손패·교역소 카드의 작은 문자 기호를 그림 아래 별도 이동 띠로 교체했다. 신발·자동차·배·비행기를 로컬 SVG로 그리고 한글 이름을 함께 표시한다. 동일 이동은 `×2`로, 서로 다른 이동은 각각 표시한다. 공포 카드도 `신발 · 도보`를 명시한다.
- 카드 높이를 늘려 그림·이동·효과·가격/점수 영역을 구분했다. 카드 접근성 이름에도 이동 수단을 추가했다. 카탈로그 이동 값·서버 지불·선택·효과음은 변경하지 않는다. 신규 dependency와 외부 이미지 사용은 없다.
- 정적 렌더 검사는 네 이동 유형·반복 수량·접근성 설명을 확인한다. 브라우저는 손패와 시장 모든 카드의 이동 SVG·한글 이름·내부 넘침을 검사한다. 2·3·4인 회귀 3건이 약 57초에 통과했고 모바일 공포·자동차와 시장 복합 이동/비행기 두 개를 이미지로 확인했다. 일회성 검증 서버는 종료했다.
- 루트 타입 검사는 통과했다. 웹 빌드는 완료됐으나 전체 빌드는 동시 작업 중인 Speakeasy의 공유 Restaurant 스키마 참조 오류로 실패했다(기존 번들 경고 유지). 전체 test는 공유 144·웹 982건 통과, 서버 4,105건 중 4,104건 통과·Speakeasy 일반 장소 점유/Restaurant 결제 테스트 1건 실패로 종료됐다. 후속 Space Crew E2E는 실행되지 않았다. 아르낙 브라우저 검사는 새 웹 빌드와 별도 서버로 통과했으며 범위 밖 코드는 수정하지 않았다. 변경 범위 공백·브라우저 구문 검사도 통과했다.

## 연구 트랙 비용·보상 시각화 — 2026-09-16

- 기존 작은 비용 텍스트 위주의 연구 트랙을 별도 `ArnakResearchTrack`으로 분리했다. 석판 느낌의 칸에 금화·나침반·석판·화살촉·보석 SVG와 수량을 표시하고, 자원 이름 범례를 제공한다. 지불 비용·남아 있는 선착순 보너스·돋보기/수첩 보상을 분리하며 점수 배지는 최종 위치 점수임을 안내한다.
- 돋보기/수첩의 단계별 보상은 공유 카탈로그 효과를 사용한다. 고용·승급·무료 유물·수호자 극복은 짧은 이름과 상세 툴팁을 제공한다. 연구 말 위치·선택 가능 강조·선택/확정 흐름과 서버 비용 판단은 유지한다. 사원 도착 칸의 비용도 숨기지 않는다.
- 연구 열 폭을 늘리고 모바일에서도 한 줄의 연속된 트랙으로 표시한다. 자원 아이콘에는 접근성 이름과 수량을 붙인다. 규칙·서버·DTO·dependency 변경은 없다.
- 정적 검사는 비용의 자원/수량·0 자원 제외, 두 말의 7단계 보상, 점수와 보너스 소진 표시를 확인한다. 브라우저 검사에 9단계·14개 보상 영역, 1L 비용 아이콘, 데스크톱/모바일 내부 넘침과 스크린샷을 추가했다. 실제 이미지에서 비용·보상·말 위치를 확인했다.
- 최초 2인 대국은 기존 유물 검사기가 `보물 상자`의 카드 뽑기 선택 전에 후속 금화 보상을 기대해 실패했다. 실제 카드 뽑기를 선택하고 손패 증가를 검사한 뒤 자원 보상을 대조하도록 보강했다. 해당 실패는 연구 트랙의 렌더·비용 검사를 통과한 뒤 발생했다.
- 재실행에서 2·3·4인 브라우저 3건이 약 63초에 통과했다(실패·스킵 0). 재실행의 유물은 다른 카드여서 보물 상자 분기 재현까지 확인한 것은 아니다. 검증용 서버·브라우저는 종료했으며 사용자의 게임 서버는 재시작하지 않았다.
- 루트 타입 검사·빌드·전체 테스트 통과: 공유 145·웹 987·서버 4,140·E2E 155, 총 5,427건 실패 0건. 기존 번들 크기 경고는 유지된다. 변경 파일 공백 검사와 브라우저 스크립트 구문 검사도 통과했다.
